#include <stdio.h>

void account() {
    printf("Tell me what you want your password to be: ");
    scanf("%s", passwords[0]);
    printf("Your password is %s\n", passwords[0]);
    printf("What is your name: ");
    scanf("%s", names[0]);
    printf("Your name is %s\n", names[0]);
}

int main(){
    Bank():
    return 0;
}